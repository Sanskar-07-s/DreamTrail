specVersion: "v1alpha"
serviceId: "dreamtrail-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dreamtrail-db"
      cloudSql:
        instanceId: "dreamtrail-instance"
